#!/usr/bin/env bash
set -e

BIN="/home/apple-pie/SLAM/fr-lio/codes/frlio/ros_app/build/devel/lib/frlio/frlio_ros_online"

if [[ ! -x "$BIN" ]]; then
  echo "ERROR: executable not found or not executable: $BIN" >&2
  exit 1
fi

exec "$BIN" "$@"
